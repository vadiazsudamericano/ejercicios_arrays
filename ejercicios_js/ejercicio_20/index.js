'use strict'
let numbers=[10,20,30,40,50];
    if (numbers[0]>numbers[4]){
        alert ('El primer índice es mayor que el último');
    }else {
       alert ('El primer índice no es mayor que el último');
    }
